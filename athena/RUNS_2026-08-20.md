# Athena run manifest, 2026-08-20

Cluster root: `/work/joy/x-vla-workshop`

All capability evaluations use official LIBERO-Object initial states, 50 episodes per task,
280 steps, 10 settle steps, an eight-action receding horizon, and the corrected 180-degree image
rotation. The four task shards are `[0,3)`, `[3,6)`, `[6,8)`, and `[8,10)`.

## Validation and matched capability

| Purpose | Slurm jobs | Current status |
|---|---:|---|
| conventional end-to-end smoke | 830459 | completed |
| conventional seed 0, four canonical shards | 830463, 830461, 830464, 830462 | completed, 449/500 (89.8%) |
| rational χ seed 0 smoke | 830465 | completed |
| rational χ seed 0, four canonical shards | 830493, 830491, 830494, 830492 | completed, 426/500 (85.2%) |
| matched χ/conventional training-step profile | 830495 | completed |
| eager and compiled same-GPU inference profile | 830516, 830544, 830545 | A30 and A6000 complete |
| all-A6000 matched capability, both architectures | 830529, 830530, 830533, 830534 plus verified base shards | completed, 422/500 χ versus 451/500 conventional |
| all-A30 hardware-replication capability | 830536, 830539, 830540, 830543 plus verified base shards | completed, 426/500 χ versus 448/500 conventional |
| all-A40 matched capability fallback | 830546–830553 | χ complete at 423/500, conventional 264/300 so far |

The first sharded Athena pass did not reproduce the earlier Modal tie. Complete same-hardware
controls now resolve the ambiguity. On A30, χ reaches 426/500 (85.2%) and the conventional twin
448/500 (89.6%), a 4.4-point deficit. On A6000, they reach 422/500 (84.4%) and 451/500 (90.2%), a
5.8-point deficit. Stratified trial-bootstrap intervals are [-8.0, -0.8] and [-9.4, -2.2] points.
These intervals cover fixed-checkpoint canonical states, not training-seed uncertainty. Task 3
accounts for 21 lost χ successes on both platforms. The exact-tie claim is rejected.

The conventional model has 20,138,632 parameters and the χ model has 20,137,352, a difference of
1,280 parameters (0.006%). On one A30 and fixed batch of 128, the χ training step is 3.12 times
slower and uses 1.63 times the peak allocated memory of the conventional twin.

The corrected same-A30 inference profile measures 38.05 ms for χ and 7.14 ms for the conventional
twin in eager mode, a 5.33-times gap. After redirecting Triton and Inductor caches from the full
home filesystem to `/work`, `torch.compile` reduces these latencies to 1.52 and 0.88 ms. The
compiled gap is 1.74 times. The initial quota failure is retained as
`inference_profile_pair_quota_failure.json`.

The A6000 replication measures a 3.49-times eager gap and a 1.73-times compiled gap. Compiler
optimization therefore leaves a consistent 1.73 to 1.74-times batch-one latency ratio across the
two tested GPU classes.

## Robustness and exactness

| Purpose | Slurm jobs | Current status |
|---|---:|---|
| paired causal language intervention, χ seeds 0, 1, 2 | 830466, 830467, 830523 | completed |
| paired causal language intervention, conventional seed 0 | 830525 | completed |
| all-layer exact attention audit, χ seeds 0, 1, 2 | 830469, 830470, 830524 | completed |
| coefficient-surgery smoke | 830475 | completed |
| coefficient-surgery discovery sweep | 830476–830490 | completed |

The surgery sweep crosses joint blocks `{0,2,4,6,7}` with ranks `{64,128,256}` on discovery tasks
`[0,4)`. Each configuration compares baseline, keep-top, Frobenius-matched keep-random,
remove-top, and Frobenius-matched remove-random. The advance gate is at least 15 percentage points
in both prespecified directions, followed by held-out task confirmation.

All fifteen configurations completed. None passed both gates. The best minimum advantage is block 0,
rank 128: keep-top beats matched random by 10 points, while removing the selected directions is 40
points more damaging than matched random removal. Because the keep margin misses the 15-point gate,
there is no held-out confirmation candidate and no successful selective coefficient edit.

The all-layer audit reconstructs four vision and eight joint attention modules per checkpoint,
or 384 heads across three checkpoints. Worst relative $\ell_2$ error is $2.56\times10^{-7}$.
Two checkpoints meet the original $2\times10^{-5}$ absolute indicator. Seed 2 reaches
$2.23\times10^{-5}$ absolute error, and that miss is retained rather than rounded away.

Across the three rational checkpoints, renaming the target shifts relative end-effector
preference by 0.168 to 0.189 m and moves the trajectory toward the renamed object in 84 to 88
percent of 100 paired rollouts. Only 28 to 33 percent end closer to it. The conventional seed-0
twin has a larger 0.239 m shift, a 92 percent directional response, and a 33 percent endpoint
rate. The grounding response is therefore not unique to decomposability.

## Additional conventional seeds

| Purpose | Slurm jobs | Current status |
|---|---:|---|
| native training smoke | 830499, 830526 | exact one-update rerun completed |
| conventional 40k-step training, seeds 1 and 2 | 830500, 830501 | completed |
| conventional seed 1, four canonical shards | 830503, 830505, 830504, 830502 | completed, 45/500 |
| conventional seed 2, four canonical shards | 830506–830509 | completed, 41/500 |

Both new training runs completed without a runtime failure and reached low final-minibatch losses. Their
closed-loop policies nevertheless collapsed outside a small number of tasks. Seed 1 reaches 9.0
percent and seed 2 reaches 8.2 percent, compared with 89.8 percent for seed 0. These runs fill the
missing job slots but reveal severe closed-loop seed sensitivity. They do not support a stable conventional
baseline, and they should not be pooled into a conversion-cost claim without matched capability
evaluation of the corresponding χ seeds and a training-recipe audit.

## Aggregation

| Purpose | Slurm job |
|---|---:|
| initial matched, causal, exact, and profile summary | 830496 canceled after failed dependency, replacement pending |
| coefficient-surgery discovery summary | 830497 |
| final three-seed conventional summary | 830510 |
| post-profile final summary | 830517 |

Raw result JSON files and per-episode records are written to
`/work/joy/x-vla-workshop/results`. Logs are written to `/work/joy/x-vla-workshop/logs`.

Seed-2 causal job 830468 and exact-audit job 830471 failed during checkpoint deserialization because
the first Modal download was corrupt. The source checkpoint was downloaded again, its ZIP payload
and SHA-256 checksum were verified, and the evaluations were resubmitted as jobs 830523 and 830524.
No failed-run measurements are included.

The surgery sweep and new conventional training jobs were released after the two complete
same-hardware capability matrices finished.

Trainer review found that the first smoke's `--steps 1` loop executed two updates. The loop and
throughput denominators now use exactly the requested update count. Job 830526 confirms the
corrected one-update behavior, and full jobs 830500 and 830501 subsequently completed.

## Follow-up jobs submitted 2026-08-21

| Purpose | Slurm jobs | Current status at submission |
|---|---:|---|
| Athena-native visual-subspace smoke | 830585 | completed, pipeline-only smoke |
| χ seeds 1 and 2, task-3 canonical evaluation | 830586, 830587 | completed, both 41/50 |
| visual-subspace intervention, χ seeds 0, 1, 2 | 830588–830590 | completed, full 47/60, selected 29/60, random 0/60 |
| cross-suite smokes on Spatial, Goal, and LIBERO-10 | 830591–830593 | completed |
| zero-shot cross-suite χ and conventional controls | 830594–830599 | queued after smokes |
| normalization partial-conversion smoke, train, and task-3 evaluation | 830602–830607 | both smokes complete, full training queued |
| fixed-cache offline checkpoint diagnostics, χ and conventional seeds 0, 1, 2 | 830608–830613 | completed |
| full canonical capability, χ seeds 1 and 2 | 830614–830621 | seed 1 complete at 404/500, seed 2 at 260/300 so far |

The visual-subspace replication rebuilds a rank-64 gripper-sensitive Gram independently for each
checkpoint, then evaluates full, selected, and fixed-random projections over four tasks and five
matched canonical trials per task. The cross-suite runs are explicitly zero-shot controls for the
Object-trained checkpoints, not a substitute for multi-suite training.

The partial-conversion controls separate bilinear attention and FFNs with ordinary per-token RMS
normalization from softmax attention and SwiGLU with rational normalization. The offline diagnostics
use the same fixed 4,096 training-cache samples for every checkpoint. They test checkpoint integrity
and whether low behavior-cloning error can coexist with closed-loop collapse. They do not distinguish
covariate shift from held-out generalization failure.

The task-3 replication reverses the seed-0 comparison. χ seeds 1 and 2 each reach $41/50$, while
the corresponding conventional checkpoints reach $37/50$ and $33/50$. Task 3 is therefore a
gap whose direction is not consistent across seeds, rather than a consistent cost of the rational architecture. Full-suite χ
evaluations remain necessary because both new conventional checkpoints fail most other tasks.

The three-checkpoint visual-subspace result has the same selected-versus-random direction at every
checkpoint. Full, selected, and random conditions reach 14/20, 8/20, and 0/20 for seed 0, 19/20,
12/20, and 0/20 for seed 1, and 14/20, 9/20, and 0/20 for seed 2. The selected subspace is causal,
but it remains 30 points below full in the pooled descriptive comparison. Near-lossless preservation
does not replicate. The completed offline values reuse the discovery sample, so three corrected
disjoint-sample diagnostics are queued as jobs 830677–830679.

The full χ seed-1 evaluation reaches 404/500, or 80.8 percent. Conventional seeds 1 and 2 have
training-cache normalized MSE of $9.51\times10^{-4}$ and $1.19\times10^{-3}$ despite their 9.0 and
8.2 percent closed-loop scores. This rules out gross checkpoint corruption but does not diagnose
the generalization failure. The A40 χ portability control reaches 423/500, or 84.6 percent, between
the A30 and A6000 matrices. The conventional A40 matrix is incomplete.

## Submitted follow-up matrices

The conventional seed comparison mixes checkpoint provenance. The strong seed-0 checkpoint is a
legacy artifact, while seeds 1 and 2 were trained by the Athena-native trainer. The executable
`launch_native_provenance.sh` submitted four training jobs concurrently: conventional seed 0
(830624) and χ seeds 0, 1, and 2 (830625–830627). Jobs 830628–830643 dependency-queue sixteen
canonical evaluation shards, or 2,000 trials, from checkpoints produced by the same trainer.

The zero-shot cross-suite jobs cannot establish in-domain multi-suite capability. The executable
`launch_indomain_multisuite.sh` is now active. Jobs 830644–830646 build Spatial, Goal, and
LIBERO-10 caches concurrently. Six matched seed-0 trainings are jobs 830647, 830652, 830657,
830662, 830667, and 830672. Their 24 dependent evaluation shards are jobs 830648–830651,
830653–830656, 830658–830661, 830663–830666, 830668–830671, and 830673–830676. Seeds 1 and 2
should be promoted only on suites where at least one seed-0 architecture reaches 50 percent.

The Oxford VPN was restored and all submissions above were confirmed through Slurm. Queue
disappearance is not treated as completion without both `sacct` state and a result artifact.

## Positive-evidence tests prepared 2026-08-21

The restricted zero-shot boundary controls map unseen instruction words to padding and are not
eligible as evidence for broad generalization. They remain in the raw result archive but are left
out of the workshop paper unless a zero-shot claim is made.

`launch_positive_evidence.sh` specifies two tests before looking at their outcomes. The ensemble
test reports both arithmetic-mean and coordinate-median aggregation for the same three fixed χ
checkpoints. The primary outcome is arithmetic mean over all 500 canonical trials. It is positive
if it exceeds the 85.3 percent descriptive mean of the three individual checkpoints. Exceeding the
best individual checkpoint's 90.0 percent is the stronger gate. Both reductions and every task are
reported, so the better reduction will not be selected post hoc.

The visual-subspace test estimates a trace-balanced action-Jacobian Gram on training-cache tasks 0
to 3, using four Rademacher probes per action coordinate. Offline evaluation uses a disjoint cache
sample. Closed-loop confirmation uses tasks 4 to 7, which were not used to choose the method, with
three independently sampled equal-rank random controls. Ranks 64, 96, 128, and 192 are all reported.
The primary positive gate is at least 90 percent retention conditional on full-policy successes and
at least a 15-point advantage over the median random control, with the same direction in all three
checkpoints. A separate offline-only rank-64 run with five random controls repairs the earlier
disjoint-sample path failure without reusing its values.

The jobs are submitted. Ensemble and balanced-subspace smokes are 830684 and 830685. The four
mean-ensemble shards are 830686–830689, and the four median-ensemble shards are 830690–830693.
Corrected offline-only gripper diagnostics are 830694–830696. The balanced confirmation sweep is
830697–830708, ordered by ranks 64, 96, 128, and 192, with seeds 0, 1, and 2 inside each rank.
All full jobs depend on their corresponding smoke passing.

## CP-term compression pilot prepared 2026-08-21

`launch_cp_pruning.sh` preregisters a weight-only structured-compression test on the fixed χ seed-2
checkpoint, which reaches 450/500 before pruning. Each bilinear FFN component is scored by the
product of its homogeneous left-row norm, homogeneous right-row norm, and down-column norm. This
score is invariant to the usual reciprocal rescaling of CP factors. The lowest-scoring 25, 50, 60,
67, and 75 percent of terms are removed from every bilinear FFN and compared with three independently
sampled matched random masks at each fraction.

The pilot uses all ten Object tasks and the first five canonical states per task. The prespecified
positive gate is that magnitude pruning retains at least 90 percent of the unpruned successes and
exceeds the median matched-random success rate by at least 15 percentage points at one removal
fraction. All five fractions and all fifteen random controls are reported. A passing fraction is then
confirmed on all 500 canonical trials and the other two χ checkpoints. This is a structured FFN
compression claim, not whole-model parameter compression, until compact modules are materialized
and timed.

The smoke is job 830709. The original 25, 50, and 75 percent pilots are jobs 830710 to 830721.
Before any pilot outcome was available, the 60 and 67 percent transition points were added as jobs
830722 to 830729. At 50 percent CP-term removal, the equivalent removable count is 5,983,488
parameters, or 29.7 percent of the complete policy.

The 25 percent result completed at 41/50, compared with 44/50 for the unpruned checkpoint on the
same canonical states. Three random masks reach 38/50, 36/50, and 41/50. This passes the 90 percent
retention condition but not the 15-point selectivity gate. While the 50 percent magnitude run was
still active, its first eight tasks showed a sharp collapse. The transition search was therefore
adaptively expanded to 30, 35, 40, and 45 percent as jobs 830746 to 830761. These new points are
exploratory discovery. Any chosen fraction still requires the prespecified blind confirmation.

Three fixed convolutional checkpoints were copied from the project volume to Athena and verified
byte-for-byte by SHA-256. The strongest one has a previously completed 472/500 evaluation.
`launch_conv_cp_pruning.sh` adds an Athena-native 50-state baseline plus a 25, 30, 35, 40, 45, and
50 percent magnitude-pruning curve. This is an architecture-replication screen. Matched random
controls will be run only at a selected fraction, followed by all-state confirmation.

The same verified convolutional checkpoints are queued for an independent ensemble replication.
Arithmetic mean is the primary outcome because these checkpoints previously reached 481/500 by
mean aggregation under the matched Modal protocol. Coordinate median is a prespecified secondary
outcome. Both reductions use four Athena shards totaling 500 canonical trials, and every task is
reported. This test is confirmatory rather than a new model-selection search.

An exploratory corrected-ensemble test uses three previously produced low-dosage corrective
checkpoints. Seeds 0 and 2 improved on their task-3/task-5 pilot slice, while seed 1 regressed.
Those pilot outcomes are not the paper result. The new test averages all three fixed checkpoints
and evaluates all 500 canonical states once. The unmodified convolutional ensemble is the fixed
comparator. Every task is reported, including the eight tasks not used for corrective data, so a
targeted gain cannot hide collateral capability loss.

## In-domain trainer compatibility repair

Jobs 830647, 830652, and 830657 exited before their first update because the newly added
`vision_encoder` argument temporarily broke the positional `make_config` call used by
`train_checkpoint.py`. This was an infrastructure failure, not an experimental outcome. The
helper signature is backward compatible again, and job 830662 reached step zero successfully
after the fix. Exact replacements are 830781 for Spatial χ, 830782 for Spatial conventional, and
830783 for Goal χ. Their original evaluation shards were rebound to the replacement dependencies.
