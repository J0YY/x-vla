"""Repository-local command and certification helpers.

Keeping this directory as an explicit package prevents an unrelated installed
package named ``scripts`` from shadowing the launch guard before source
verification runs.
"""
